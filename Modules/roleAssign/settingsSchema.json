{
  "id": "role-assign-settings",
  "version": "1.0.0",
  "name": "Role Assignment",
  "description": "Configure role assignment system settings",
  "icon": "🎭",
  "scope": "both",

  "sections": [
    {
      "id": "limits",
      "name": "Limits",
      "description": "Configure limits for role assignment groups",
      "icon": "🔢",
      "order": 1
    },
    {
      "id": "feedback",
      "name": "Feedback",
      "description": "Configure feedback messages when roles are assigned or removed",
      "icon": "💬",
      "order": 2
    }
  ],

  "settings": {
    "limits.maxGroups": {
      "type": "number",
      "default": 8,
      "label": "Max Groups",
      "description": "Maximum number of role assignment groups allowed per guild",
      "section": "limits",
      "order": 1,
      "validation": {
        "min": 1,
        "max": 50,
        "absoluteMin": 1
      }
    },
    "feedback.enabled": {
      "type": "boolean",
      "default": true,
      "label": "Enable Feedback",
      "description": "Show feedback messages when roles are assigned/removed (buttons: ephemeral, reactions: DM)",
      "section": "feedback",
      "order": 1
    },
    "feedback.reactionDM": {
      "type": "boolean",
      "default": true,
      "label": "Reaction DM Feedback",
      "description": "Send DM feedback for reaction-based role assignments (if disabled, reactions are silent)",
      "section": "feedback",
      "order": 2
    },
    "feedback.roleAdded": {
      "type": "string",
      "default": "✅ Added {role} to {user}",
      "label": "Role Added Message",
      "description": "Message shown when a role is added. Variables: {role}, {user}",
      "section": "feedback",
      "order": 3,
      "validation": {
        "required": true,
        "minLength": 1,
        "maxLength": 200
      }
    },
    "feedback.roleRemoved": {
      "type": "string",
      "default": "❌ Removed {role} from {user}",
      "label": "Role Removed Message",
      "description": "Message shown when a role is removed. Variables: {role}, {user}",
      "section": "feedback",
      "order": 4,
      "validation": {
        "required": true,
        "minLength": 1,
        "maxLength": 200
      }
    },
    "feedback.alreadyHasRole": {
      "type": "string",
      "default": "⚠️ You already have a role from this group",
      "label": "Already Has Role Message",
      "description": "Message shown when user already has a role (single selection mode)",
      "section": "feedback",
      "order": 5,
      "validation": {
        "required": true,
        "minLength": 1,
        "maxLength": 200
      }
    }
  }
}
